<style is:global>
    .bare::before { content:'<click-highlight>' }
    .bare::after { content:'</click-highlight>' }

    .bare::before, *::after {
    color:rgba(136, 18, 128, 0.5);
    font-weight:100;
    font-size:1.0em;
    }
</style>